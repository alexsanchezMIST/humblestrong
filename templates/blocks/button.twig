<a href="{{ fields.link }}" target="_blank" class="btn btn--primary">
    {% if fields.icon %}
    <span class="button-icon">{{ Image(fields.icon).src }}</span>
    {% endif %}
    {{ fields.text }}
</a>